machine:
  python:
    version: 2.7.6
general:
  branches:
    only:
      - master
      - circleci-testing
dependencies:
  pre:
    - sudo apt-get screen
    - sudo apt-add-repository ppa:bitcoin/bitcoin -y
    - sudo apt-get update -q
    - sudo apt-get install --no-install-recommends --no-upgrade -qq bitcoind
  override:
    - pip install -r requirements.txt
test:
  pre:
    - mkdir /home/circleci/.bitcoin
  override:
    - trial p2pool
